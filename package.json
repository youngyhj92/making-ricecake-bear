{
  "name": "making-ricecake-bear",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "build": "node scripts/build.mjs",
    "preview": "node scripts/dev-server.mjs dist",
    "deploy": "npm run build && npx --yes wrangler pages deploy dist --project-name making-ricecake-bear --branch main"
  }
}
